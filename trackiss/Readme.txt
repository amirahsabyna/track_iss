Instruction:

1. Install: 
    1) python
    2) pip install turtle
    3) pip install urllib3
    4) pip install times
    5) pip install geocoder
    6) pip install geopy

2. Run code and then the result will appear on terminal